# Honeycomb 🍯

一个基于 Model Context Protocol (MCP) 的服务配置管理平台，提供可视化的 MCP 服务配置和管理功能。

## 📋 项目简介

Honeycomb 是一个 monorepo 项目，用于管理和配置 MCP 服务。它提供了：

- 📊 **可视化配置界面**：基于 Vue.js 3 和 Element Plus 的现代化管理界面
- 🔧 **服务管理**：支持 MCP 服务的创建、编辑、启动和停止
- 🛠️ **工具配置**：灵活的工具（Tools）配置，支持自定义回调函数
- 📚 **API 文档**：集成 Swagger UI，提供完整的 API 文档
- 💾 **本地数据库**：使用 SQL.js 实现的本地数据持久化

## 🏗️ 项目结构

```
honeycomb/
├── apps/
│   ├── client/          # Vue.js 3 客户端应用
│   ├── server/          # Express 服务器
│   └── database/        # 数据库模块
├── packages/
│   └── honeycomb-type/  # 共享 TypeScript 类型定义
└── scripts/             # 构建脚本
```

### 应用说明

#### `apps/client`

Vue.js 3 前端应用，提供 MCP 服务配置的可视化管理界面。

**主要功能：**

- 服务配置列表展示
- 服务的增删改查
- 服务启动/停止控制
- 实时状态监控

#### `apps/server`

Express 服务器，提供 REST API 和 MCP 服务管理功能。

**主要功能：**

- REST API 路由处理
- MCP 服务实例管理
- SSE (Server-Sent Events) 支持
- Swagger API 文档

#### `apps/database`

数据库模块，使用 SQL.js 和 Kysely 实现本地数据持久化。

**主要功能：**

- 数据库表结构定义
- 数据访问层封装
- 数据库初始化

#### `packages/honeycomb-type`

共享的 TypeScript 类型定义包，供所有应用使用。

## 🚀 快速开始

### 前置要求

- Node.js >= 18
- pnpm >= 8

### 安装依赖

```bash
# 安装所有工作空间的依赖
pnpm install
```

### 开发模式

#### 启动客户端开发服务器

```bash
cd apps/client
pnpm dev
```

客户端将在 `http://localhost:5173` 运行（默认 Vite 端口）。

#### 启动服务器

```bash
cd apps/server
pnpm build
pnpm start
```

或者使用开发模式（需要配置相应的构建脚本）：

```bash
# 如果需要，先构建数据库和类型包
cd apps/database && pnpm build
cd ../../packages/honeycomb-type && pnpm build
cd ../../apps/server
pnpm build && pnpm start
```

服务器将在 `http://0.0.0.0:3002` 运行。

#### 访问应用

- **Web 界面**：http://localhost:3002
- **API 文档**：http://localhost:3002/api-docs

### 构建生产版本

```bash
# 构建所有包
pnpm -r build

# 或单独构建
cd apps/client && pnpm build
cd ../server && pnpm build
```

## 📦 技术栈

### 客户端

- **Vue.js 3**：渐进式 JavaScript 框架
- **Element Plus**：Vue 3 组件库
- **Vite**：下一代前端构建工具
- **TypeScript**：类型安全的 JavaScript

### 服务器

- **Express**：Node.js Web 框架
- **Model Context Protocol SDK**：MCP 协议实现
- **Swagger UI**：API 文档生成和展示
- **Consola**：优雅的终端日志输出

### 数据库

- **SQL.js**：SQLite 的 WebAssembly 实现
- **Kysely**：类型安全的 SQL 查询构建器

### 工具链

- **pnpm workspace**：Monorepo 管理
- **unbuild**：统一的构建工具
- **TypeScript**：类型检查

## 🔧 配置说明

### MCP 服务配置

每个 MCP 服务配置包含以下信息：

- **基本信息**：名称、版本、描述
- **状态**：运行中 (running) / 已停止 (stopped)
- **工具列表**：每个工具包含：
  - 名称和描述
  - 输入/输出 Schema（JSON Schema 格式）
  - 回调函数代码

### 数据库初始化

数据库文件位于 `apps/database/mcp.db`。首次运行时，数据库会自动初始化。

如需手动初始化：

```bash
cd apps/database
pnpm build
pnpm init-db
```

## 📖 API 文档

启动服务器后，访问 `http://localhost:3002/api-docs` 查看完整的 API 文档。

主要 API 端点：

- `GET /api/v1/configs` - 获取所有配置列表
- `GET /api/v1/configs/:id` - 获取指定配置详情
- `POST /api/v1/configs` - 创建新配置
- `PUT /api/v1/configs/:id` - 更新配置
- `DELETE /api/v1/configs/:id` - 删除配置
- `POST /api/v1/configs/:id/start` - 启动服务
- `POST /api/v1/configs/:id/stop` - 停止服务

## 🤝 开发规范

### 代码风格

项目使用 Prettier 进行代码格式化：

```bash
pnpm format
```

### 类型检查

客户端类型检查：

```bash
cd apps/client
pnpm type-check
```

### 版本管理

使用项目提供的版本管理脚本：

```bash
pnpm bumpp
```

## 📝 许可证

ISC

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📮 联系方式

如有问题或建议，请通过 Issue 联系我们。

---

Made with ❤️ by JD WMFE Team
